[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.server"

[deploy]
startCommand = "chaosguard-server"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
